# dependencies
node_modules/

# expo
.expo/
dist/
web-build/
expo-env.d.ts

# native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macos
.DS_Store
*.pem

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo

# Firebase service account secrets (should not be committed)
firebase-adminsdk-*.json
*-firebase-adminsdk-*.json
*serviceaccount*.json
serviceAccount.json
firebase-service-account.json

# If you accidentally add a google-services.json for production, you may still want to allow it
# but ensure secrets are stored via EAS secrets or env; keep it tracked only if safe.
